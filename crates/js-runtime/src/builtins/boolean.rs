//! `Boolean` global / prototype / constructor.
//!
//! TODO: spec-conformant builtins for the `boolean` family.

use crate::realm::Realm;

/// Install the boolean built-ins into `realm`.
pub fn install(_realm: &mut Realm) {
    // TODO: prototype + constructor + prototype methods.
}
