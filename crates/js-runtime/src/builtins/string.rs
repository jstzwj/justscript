//! `String` global / prototype / constructor.
//!
//! TODO: spec-conformant builtins for the `string` family.

use crate::realm::Realm;

/// Install the string built-ins into `realm`.
pub fn install(_realm: &mut Realm) {
    // TODO: prototype + constructor + prototype methods.
}
