//! `Number` global / prototype / constructor.
//!
//! TODO: spec-conformant builtins for the `number` family.

use crate::realm::Realm;

/// Install the number built-ins into `realm`.
pub fn install(_realm: &mut Realm) {
    // TODO: prototype + constructor + prototype methods.
}
