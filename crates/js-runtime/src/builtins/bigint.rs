//! `Bigint` global / prototype / constructor.
//!
//! TODO: spec-conformant builtins for the `bigint` family.

use crate::realm::Realm;

/// Install the bigint built-ins into `realm`.
pub fn install(_realm: &mut Realm) {
    // TODO: prototype + constructor + prototype methods.
}
